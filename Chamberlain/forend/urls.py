from django.contrib import admin
from django.urls import path
import forend.views

urlpatterns = [
    # path('hello', backend.views.index),
    # path('content', backend.views.article_content),
    # path('index', backend.views.get_index_page),
    # # path('detail', backend.views.get_detail_page),
    # path('detail/<int:article_id>', backend.views.get_detail_page),
    # path('404', backend.views.not_find_page),
    # path('edit', backend.views.edit_page),
    # path('edit/0', backend.views.edit),
    # path('image', backend.views.image),
    # path('image1', backend.views.ImageView.as_view()),  # 视图类
    # path('imagetext', backend.views.ImageText.as_view()), # 视图类
    # path('cookietest', backend.views.CookieTest.as_view()),# 视图类
    # path('cookieaccept', backend.views.CookieAccept.as_view()),# 视图类
    # path('authorize', backend.views.Authorize.as_view()),# 视图类
    # path('logout', backend.views.Logout.as_view())# 视图类


]

